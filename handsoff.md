# shefutech NewsHub — 引き継ぎドキュメント

**作成日:** 2026年8月20日
**目的:** 別スレッドへの移行にあたり、これまでの経緯・アーキテクチャ・重要な制約・学びを引き継ぐ

---

## 1. プロジェクト概要

**shefutech NewsHub** — Yuta（Cybereason Senior SE）が個人開発する、バイリンガル（日本語/英語）サイバーセキュリティ×AIニュースアグリゲーター。

- **デプロイ先:** `cloudflare-news-platform.shefutech.workers.dev`
- **リポジトリ:** `shefuyuta/cloudflare-news-platform`
- **開発環境:** Windows 11 / PowerShell / VSCode
- **デプロイフロー:** ps1パッチ適用 → VSCodeでcommit・push → GitHub↔Cloudflare自動デプロイ（手動build/deploy不要）

## 2. 技術スタック

- **フロントエンド:** Next.js 15 App Router + `@opennextjs/cloudflare`、Tailwind CSS v4、TypeScript
- **インフラ:** Cloudflare Workers / D1（SQLite、DB名`newshub`、ID `a18b94b2-1c96-4d92-bfbd-87581ac2f2c9`）/ Vectorize（`newshub-index`、1024次元、bge-m3多言語埋め込み）/ Workers AI（`llama-3.3-70b-instruct-fp8-fast`：LLM、`bge-m3`：埋め込み）
- **RAG構成:** `rag_config`テーブルの値がコードのデフォルトを上書き（`min_score`, `top_k`等はDB管理、`system_prompt`はDBに無ければコードのデフォルト使用）

## 3. 極めて重要な制約（絶対に忘れてはいけない）

1. **Cloudflare Workersのsubrequest上限は約1000/invocation。** 記事ごとに複数回API呼び出しをするループは、必ず件数上限を設ける（今日、関連記事検出機能でこの上限を踏み抜き、全cronが「Exceeded Resources」で失敗する障害を起こした）。
2. **Cloudflare Workers Freeプランのcron trigger上限はアカウント全体で5本。** 現在5本すべて使用中（fetch-news, ransomware-fetch, embed×2, digest）。新しいcronを追加する余地は無い。新機能はオンデマンドのDB集計（cron不要）で実現するのが基本方針。
3. **`env.AI.run()`の応答形状はモデル・プロンプトによって変わる。** `{response: string}`、`{response: object}`（JSON強制時）、`{choices: [{message: {content: string}}]}`（OpenAI形式）のいずれかで返ってくる。呼び出し側は必ず3形状すべてに対応する防御的なパースを行うこと（`lib/digest/generate.ts`の実装例を参照）。
4. **`rag_config`テーブルの値はコードのデフォルトを上書きする。** `min_score`, `top_k`はDB管理、変更時はSQL UPDATE（デプロイ不要、即反映）。
5. **`env.DB.batch()`を使った一括処理が必須。** 記事ごとの個別`.run()`はsubrequest上限にすぐ達する。
6. **サブカテゴリ分類・手口分類は共に単一ラベル方式**（最も類似度が高いラベルのみ付与、閾値0.5）。マルチラベルは意味の重複するカテゴリ同士で誤爆する（今日、vulnerability/incident分類で実際に発生し是正済み）。

## 4. 今日実装した主要機能（時系列）

### RAGチャットボットの根本改善
- 全期間検索対応（従来は24時間固定で「見つからない」を連発していた）
- キーワード統計の側路除去（「0件見つかりました」という矛盾表示の原因だった）
- ランサムウェア専用の集計ツール（`lib/rag/ransomware-tool.ts`：正確なSQL集計をLLMに渡す設計）
- `env.AI.run()`応答形状の堅牢化（3形状対応）

### ランサムページの拡張
- グループ別・業種別トレンド折れ線グラフ（月次/半月次推移）
- 急増グループ検出（直近7日 vs その前7日）
- 休止グループ検出（60-120日新規被害なしのグループ）
- 全世界/日本トグル
- 業種フィルタ新設（グループ・国フィルタと同様のAND条件）
- 「期間以外の全フィルタ（国・グループ・業種）がトレンドチャートに連動」という設計方針

### ダッシュボード
- ランサムウェア速報（7日間）+ 全世界/日本トグル
- 急上昇タグ検出（記事タグの直近7日 vs その前7日）— **トレンドタグ（`hours`可変）とは全く別の固定7日間ロジック**という重要な区別あり
- カウントアップアニメーション、棒グラフアニメーション
- ソース別テーブルに地域情報（主要カバー地域）を追加
- 各セクション見出しに集計期間を明示（「過去24時間」等）— 急増・急上昇には別途「直近7日 vs その前7日」の定義文を明記

### 新機能: AI×Securityページ（`/ai-security`）
- **重要な設計知見:** クロスラベルタグ（`AI`/`Cyber`）は記事の**主カテゴリ以外**のデスクにのみ付与される。つまり`category='ai'`の記事には`Cyber`タグが付き、`AI`タグ自体は付かない（逆も同様）。「AI×Security交差点」は`(category=ai AND Cyberタグあり) OR (category=cybersecurity AND AIタグあり)`という複合条件で表現する必要がある。
- AI攻撃手法（手口）分類: `lib/fetcher/technique-embed.ts`。4ラベル（`phishing-genai`, `deepfake`, `model-attack`, `ai-automation`）。**キーワード一次フィルタ + AI×Security交差点への厳格な絞り込み**を経て初めて実用精度に達した（当初は「Nvidia株価」等の無関係記事まで誤分類していた）。
- 手口別トレンドグラフ（半月単位、年表記なし）
- 急上昇タグ（AI×Security限定）

### ダイジェスト機能（`/digest`）
- Daily（毎日9AM JST）/ Weekly（金曜）/ Monthly（月末）
- **Cron trigger上限5本の制約により、新規cronは1本のみ**（`0 0 * * *`）。コード側で曜日・月末判定してDaily/Weekly/Monthlyを出し分け。
- 前回比較（トレンドの継続/転換を記述）、注目記事の要約言及、急増グループの標的業種に基づく控えめな示唆
- **AI生成失敗時のフォールバック文を、UIで明示的に区別**（`usedFallback`フラグ、警告バナー表示）

### UI/UX改善（多数）
- タグのAND条件フィルタ（OR条件だったバグを修正）
- タグ一覧に件数表示、リセットボタン
- 関連記事グルーピング（`related_articles`テーブル、embed時にVectorize類似検索）
- 検索結果のマッチ抜粋ハイライト（日本語の文字種境界分割対応）+ 関連度スコア表示
- 検索モードの自動フォールバック（意味検索0件→キーワード検索）を明示的にUI表示
- 保存フィルター機能（検索ページに集約、localStorage、最大3件）
- 既読記事の視認性強化、ホバーエフェクトの改善（行全体の背景色変化）
- ローディングスケルトン（全ページ共通）
- モバイルタップ領域の拡張（`min-h-[36px] sm:min-h-0`パターン）
- ヘッダー検索窓の自動クリア（ページ遷移時）

## 5. 今日発見・修正した重大バグ（教訓として重要）

1. **タグ絞り込みがOR条件だった**（複数タグ選択で件数が減らず増えていた）→ AND条件に修正
2. **「その他」サブカテゴリタブが構造的に0件だった**（存在しない`sub:other`タグを検索していた）→ `NOT EXISTS`条件に修正
3. **ダッシュボードのトレンドタグと急上昇タグの集計期間が異なるのに、リンク先のURLが同じ`hours`を使っていた** → 急上昇は固定`hours=168`にリンクを分離、UIにも期間を明示
4. **サイドバーのアクティブ状態判定が前方一致(`startsWith`)だったため、`/ai-security`が`/ai`のナビも同時にハイライトしていた** → 完全一致 or `href + "/"`の境界チェックに修正
5. **関連記事検出機能が、embedパイプラインの1ラウンドあたり全記事(最大200件)にVectorizeクエリを追加し、subrequest上限を突破してサイト全体をダウンさせた** → 1ラウンドあたり15件に上限設定

## 6. パッチ配布の運用パターン（Yutaの開発フロー）

- コード変更は base64埋め込みPowerShellスクリプト（`apply-*.ps1`）として配布（zip解凍がWindows環境で不安定だったため）
- 各パッチは roundtrip検証（base64エンコード→デコードして元ファイルと一致確認）を経てから配布
- DBスキーマ変更を伴う場合、SQLをパッチのヘッダーコメントに明記し、**必ずコード反映後すぐSQL実行するよう強く念押しする**（今回、コードだけ先にデプロイされてテーブル未作成のままサイトが落ちる事故が発生したため）

## 7. 次のフェーズ: iOSモバイルアプリ化

Yutaは、今回の一連の機能拡張を経て、次はiOSモバイルアプリ化を検討している。新スレッドで、以下を含めて検討を開始することを推奨する。

**検討すべき選択肢:**
- 既存のNext.js WebアプリをWebView的にラップする（React Native WebView, Capacitor等）— 開発コスト低、ネイティブ感は劣る
- React Nativeでネイティブアプリとして再構築 — 開発コスト高、UXは向上
- 既存のCloudflare Workers API層をそのまま活用し、UIだけネイティブで作り直す — バックエンドは流用可能

**引き継ぐべき前提:**
- バックエンド（Cloudflare Workers, D1, Vectorize, Workers AI）はそのまま使える。API化されていない部分（サーバーコンポーネントで直接DB読んでいる箇所）は、モバイルアプリ用に新規APIエンドポイントの追加が必要になる可能性が高い。
- 認証機構は現状無し（ユーザー登録なし、個人利用前提）。モバイルアプリ化にあたり、この方針を維持するか再検討するかは要相談。
- 今日実装した各種フィルタ・保存機能（localStorage依存）は、モバイルアプリでは別の永続化方式（AsyncStorage等）に置き換える設計が必要。

**新スレッドでまず確認すべきこと:**
1. このzipに含まれるコードベースの構成を把握（`app/`, `components/`, `lib/`のディレクトリ構造）
2. 上記の技術スタック・制約を踏まえた上で、iOSアプリの実現方式（WebView vs ネイティブ）をYutaと相談
3. 必要であれば、モバイル専用API（`/api/mobile/*`等）の設計から着手

---

**このドキュメントの使い方:** 新しいスレッドの最初のメッセージで、このファイルの内容とプロジェクトのzipを一緒に共有し、「このドキュメントを読んで、iOSモバイルアプリ化の方針を一緒に検討したい」と伝えてください。
